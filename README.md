# Аналитическая нейросеть для проверки студенческих работ (МИРЭА)

## Неделя 1 — MVP готов
- Загрузка и обработка .docx / .pdf
- Извлечение текста
- Базовый веб-интерфейс на Streamlit
- Поддержка метаданных работы

## Как запустить
```bash
python -m streamlit run app.py


### 4. (Опционально, но очень рекомендуется) — Инициализируем Git

В терминале выполни по очереди:

```powershell
git init
git add .
git commit -m "Week 1: MVP — загрузка файла + извлечение текста"